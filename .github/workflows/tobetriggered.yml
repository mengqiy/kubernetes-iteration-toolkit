name: Release
on:
  workflow_run:
    workflows: [ReleaseTrigger]
    types: [completed]
jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
